export default function CodeCard() {
  return (
    <pre className="code-card">
      <code>
        <span className="signal">const</span> <span className="dim">torquato</span> <span className="dim">{'= {'}</span>{'\n'}
        {'  '}<span className="dim">role:</span> <span className="signal">"tech lead"</span><span className="dim">,</span>{'\n'}
        {'  '}<span className="dim">focus:</span> <span className="signal">"ai engineering"</span><span className="dim">,</span>{'\n'}
        {'  '}<span className="dim">based:</span> <span className="signal">"canoas, rs"</span><span className="dim">,</span>{'\n'}
        {'  '}<span className="dim">shipping:</span> <span className="signal">true</span>{'\n'}
        <span className="dim">{'}'}</span>
      </code>
    </pre>
  )
}
